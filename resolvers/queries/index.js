import Info from './Info';
import Feed from './Feed';
import User from './User';

const { info } = Info
const { feed }= Feed
const { currentUser } = User;

export default {
    info,
    feed,
    currentUser
}